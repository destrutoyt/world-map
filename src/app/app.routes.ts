import { Routes } from '@angular/router';
import { WorldMap } from './world-map/world-map';

export const routes: Routes = [{
    path: '',
    title: 'World Map || Miguel G',
    component: WorldMap,
}];
